import { AddContactForm } from 'components';
import { Container } from '@mui/material';

export const AddContact = () => {
  return (
    <Container sx={{ pb: 2 }}>
      <AddContactForm />
    </Container>
  );
};
