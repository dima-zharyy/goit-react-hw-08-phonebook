import { Container } from '@mui/material';
import { SignInForm } from 'components';

export const SignIn = () => {
  return (
    <Container sx={{ pt: 2 }}>
      <SignInForm />
    </Container>
  );
};
