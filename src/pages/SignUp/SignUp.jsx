import { Container } from '@mui/material';
import { SignUpForm } from 'components';

export const SignUp = () => {
  return (
    <Container sx={{ pt: 2 }}>
      <SignUpForm />
    </Container>
  );
};
